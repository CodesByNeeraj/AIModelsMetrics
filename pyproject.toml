[project]
name = "aimodelsmetrics"
version = "0.1.0"
description = Benchmarking Claude, GPT, and Gemini on a real product task
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "anthropic>=0.97.0",
    "google-genai>=1.74.0",
    "openai-agents>=0.14.8",
]
